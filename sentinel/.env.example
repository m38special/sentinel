# SENTINEL Phase 2 — Environment Variables
# LiQUiD SOUND | Copy to .env and fill in values
# NEVER commit .env to git

# ── PumpPortal ───────────────────────────────────────────
PUMP_WS_URL=wss://pumpportal.fun/api/data
PUMP_API_KEY=                          # Optional — for higher rate limits

# ── Redis (Celery broker + cache) ────────────────────────
# Railway: interchange.proxy.rlwy.net:56657
REDIS_URL=redis://localhost:6379/0     # Local dev default

# ── TimescaleDB ──────────────────────────────────────────
TIMESCALEDB_URL=postgresql://sentinel:changeme_in_prod@localhost:5432/sentinel
TIMESCALEDB_DB=sentinel
TIMESCALEDB_USER=sentinel
TIMESCALEDB_PASSWORD=changeme_in_prod  # CHANGE IN PROD

# ── Slack ────────────────────────────────────────────────
SLACK_BOT_TOKEN=xoxb-...
SLACK_ALERT_CHANNEL=C0AHE2LQFRC       # #idea-ocean (default); change to dedicated alert channel

# ── Alert Thresholds ─────────────────────────────────────
ALERT_THRESHOLD_SLACK=70              # Score ≥ 70 → Slack alert
ALERT_THRESHOLD_ALL_CHANNELS=85       # Score ≥ 85 → Yoruichi approval gate
ALERT_THRESHOLD_URGENT=95             # Score ≥ 95 → All channels (Telegram, Discord)

# ── Filtering ────────────────────────────────────────────
MIN_MARKET_CAP_SOL=0                  # 0 = catch all new launches
MIN_VSOL_USD=0                        # Optional volume filter in USD
SOL_USD_ESTIMATE=150                  # Used for USD display only

# ── Flower (Celery UI) ───────────────────────────────────
FLOWER_USER=admin
FLOWER_PASSWORD=changeme_in_prod      # CHANGE IN PROD

# ── Misc ─────────────────────────────────────────────────
RECONNECT_DELAY_S=5
PORT=8080
